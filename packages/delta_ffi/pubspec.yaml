name: delta_ffi
description: "Delta Chat Core bindings and safe wrappers for Axichat."
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  ffi: ^2.1.3
  code_assets: ^0.19.9
  hooks: ^0.20.4
  native_assets_cli: ^0.7.2
  native_toolchain_rust: ^0.1.0-dev.7

dev_dependencies:
  ffigen: ^12.0.0
  lints: ^3.0.0
