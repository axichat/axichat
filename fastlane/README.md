# Fastlane metadata workflow
# Before running `fastlane supply`, review and update files in fastlane/metadata/android/* for every locale you ship to.
# Keep changelog files synced with the Play Console by copying TEMPLATE.txt to <versionCode>.txt for each release.
